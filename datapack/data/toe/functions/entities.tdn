@on compile
@priority 2.0


define function player {
    @tag tick
    
    as @a at @s function this {
        set @s->toe.age += 1
        
            
        
        eval Move.run(MOVES)
        eval Power.run(POWERS)
        
    }
}